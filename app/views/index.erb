<ul>
	 “Why can’t I just eat my waffle.”  -- Barack Obama
	 <img src='/images/logo.png'>
  <% @peeps.each do |peep| %>
    <li> <%= peep.message %></a></li>
    <li> <%= peep.message %></a></li>
  <% end %>
</ul>